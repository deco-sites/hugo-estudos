import Component from "$store/components/image-look/ImageLookButton.tsx";
import type { Props } from "$store/components/image-look/ImageLookButton.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
